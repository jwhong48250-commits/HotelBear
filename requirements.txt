# =============================================================================
# HotelBear — Backend API(Flask) 실행용 Python 의존성
#
# 이 프로젝트의 `BackEnd/api/app.py`는 아래 패키지만 런타임에 필요합니다.
# (학습/크롤링/가격업데이트 스크립트는 별도 실행 시 필요 패키지가 추가될 수 있음)
# =============================================================================

flask>=2.3.0
flask-cors>=4.0.0

pymysql>=1.1.0
python-dotenv>=1.0.0
